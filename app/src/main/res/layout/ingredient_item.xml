<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/linearLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="20dp">

    <!-- Заголовок -->
    <TextView
        android:id="@+id/TextViewIngredient"
        android:layout_width="350dp"
        android:layout_height="wrap_content"
        android:fontFamily="@font/archivo_black"
        android:gravity="left"
        android:text="Ingredient"
        android:textStyle="bold"
        android:textColor="@color/my_text_view_back"
        android:textSize="21sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.cardview.widget.CardView
        android:id="@+id/CardViewIngredient"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="7dp"
        android:padding="8dp"
        app:cardBackgroundColor="@android:color/transparent"
        app:cardElevation="8dp"
        app:cardCornerRadius="10dp"
        app:cardUseCompatPadding="true"
        app:layout_constraintTop_toBottomOf="@id/TextViewIngredient"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:id="@+id/LinearLayoutIngredient"
            android:layout_width="350dp"
            android:layout_height="50dp"
            android:orientation="horizontal"
            android:gravity="center"
            android:background="@drawable/textview_border">

            <EditText
                android:id="@+id/EditTextNameIngredient"
                android:singleLine="true"
                android:scrollHorizontally="true"
                android:ellipsize="none"
                android:inputType="text"
                android:layout_width="140dp"
                android:layout_height="50dp"
                android:layout_marginStart="20dp"
                android:hint="name"
                android:paddingTop="15dp"
                android:textColorHint="@android:color/darker_gray"
                android:textColor="@color/my_text_view_back"
                android:textSize="13sp" />

            <FrameLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content">

                <EditText
                    android:id="@+id/EditTextWeight"
                    android:layout_width="80dp"
                    android:layout_height="50dp"
                    android:background="@drawable/weight_border"
                    android:gravity="center"
                    android:hint="Weight"
                    android:inputType="numberDecimal"
                    android:minHeight="48dp"
                    android:textColor="@color/my_text_view_back"
                    android:textColorHint="@color/my_text_view_back"
                    android:textSize="12sp" />
            </FrameLayout>

            <FrameLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content">

                <TextView
                    android:id="@+id/TextViewUnit"
                    android:layout_width="70dp"
                    android:layout_height="50dp"
                    android:background="@drawable/weight_border"
                    android:gravity="center"
                    android:hint="kg"
                    android:inputType="numberDecimal"
                    android:minHeight="48dp"
                    android:textColor="@color/my_text_view_back"
                    android:textColorHint="#979595"
                    android:textSize="13sp" />
            </FrameLayout>

            <Spinner
                android:id="@+id/SpinnerUnit"
                android:layout_width="wrap_content"
                android:layout_height="70dp"
                android:entries="@array/ingredients_unit"
                android:minHeight="48dp" />
        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <Button
        android:id="@+id/ButtonDelete"
        style="@style/Widget.AppCompat.Button.Borderless.Colored"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="25dp"
        android:minHeight="48dp"
        android:text="Delete"
        android:textColor="@android:color/holo_red_dark"
        android:textSize="10sp"
        app:layout_constraintBaseline_toBaselineOf="@id/TextViewIngredient"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>